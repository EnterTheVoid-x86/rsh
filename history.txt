History? More like... BROWSER HISTORY-






















kljkjklj












clear
Among US?
Among
AMONG US
Among us?
clear
puts
clear
irb
sdfsdfsdf
Among us?
SUS
SUS
clear
retry
/
\
\
\
\
\
`
1
1
1
~
/
~?
/?
?
.
dcsjiudvjdvs
fv
f
f
;i
l;
iu;uiku
kuku
ktuko
gj59y
54y54y
9
9
9
.
.
.
.
/
/
/
/.
.
.
.
.
.
.
,
,
,
,
,
Z
irb
irb
irb
irb
cd /
clear
bash lambda.sh
bash lambda.sh
cd ~
cd Ruby-Shell-v131 R
cd Ruby-Shell-v131
cd /
cd/
cd /
clear
history
history
among us
amongus
clear
amongus
ls amongus
lambda
cls
beeep
beep
beep
beep
beep
lambda
cls
beep
beep
beep
beep
the fitness gram pacer test is a multistage aerobatic capacity test that progressively fucks your mom
the fitness gram pacer test is a multistage aerobatic capacity test that progressively gets more difficult as it continues
inf
inf
inf
inf
inf
info
inf
info
info
inf
date
date
date
date
date
time
time
time
time
date
time
time
time
time
time
time
time
time
time
date
date
time
time
date
date
time
time
time
cls
time
date
echo "cool"
unixtime
europedate
europedate
24hr
24hr
europedate
help
help
help
help
time
thanksgiving
thanksgiving
thanksgiving
thanksgiving
thankgsiving
thanksgiving
thankgsiving
thanksgiving
help
thanksgiving
gem 
cls
beep
hepl
help
tar -xcvz
tar --xcvz
tar --xvz
tar 
curl
curl https://www.purebasic.com/download/purebasic-demo_x64.tar.gz
curl https://www.purebasic.com/download/
ls
ls
sls
sls
ls
ls
git clone https://www.purebasic.com/download/purebasic-demo_x64.tar.gz
git clone https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
git clone https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
curl https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
git clone https://github.com/QB64Team/qb64
cd qb64
ls
ls
l
ls
ls
cd internal
ls
cd c
ls
cd ..
cd ..
make
cd internal 
cd c
make
cd ~
curl -LJQ https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
curl -LJQ https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
curl -LJO https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
tar qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz 
tar -xf https://github.com/QB64Team/qb64/releases/download/v2.0.2/qb64_2021-11-07-02-59-19_4d85302_lnx.tar.gz
;
;
calculator
calc
chmod +x calc
chmod +x calc
calc
calc
calc
calc
NUCLE198511
help
alias
ls
./posh-linux-amd64 --print-shell
ls
cd ..
ls
ls ./bashrc
cat .bashrc
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples
# for examples
case $- in
    *i*) ;;
      *) return;;
esac
esac
# See bash(1) for more options
HISTCONTROL=ignoreboth
HISTCONTROL=ignoreboth
shopt -s histappend
shopt -s histappend
# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=1000
HISTFILESIZE=2000
HISTFILESIZE=2000
# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize
shopt -s checkwinsize
# If set, the pattern "**" used in a pathname expansion context will
# match all files and zero or more directories and subdirectories.
#shopt -s globstar
#shopt -s globstar
# make less more friendly for non-text input files, see lesspipe(1)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"
# set variable identifying the chroot you work in (used in the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi
fi
# set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
    xterm-color|*-256color) color_prompt=yes;;
esac
esac
# uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
#force_color_prompt=yes
#force_color_prompt=yes
if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
    # (ISO/IEC-6429). (Lack of such support is extremely rare, and such
    # a case would tend to support setf rather than setaf.)
    color_prompt=yes
    else
    color_prompt=
    fi
fi
fi
if [ "$color_prompt" = yes ]; then
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
fi
unset color_prompt force_color_prompt
unset color_prompt force_color_prompt
# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    ;;
*)
    ;;
esac
esac
# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'
    #alias vdir='vdir --color=auto'
    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi
fi
# colored GCC warnings and errors
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias l='ls -CF'
# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
# Alias definitions.
# You may want to put all your additions into a separate file like
# ~/.bash_aliases, instead of adding them here directly.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi
fi
# sources /etc/bash.bashrc).
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
ficls
cls
cls
cls
cls
cls
nano
nano
cd Ruby-Shell-v137 
dpkg-deb
dpkg-deb --help
dpkg-deb -x nano_5.4-2_amd64.deb 
dpkg-deb -x nano_5.4-2_amd64.deb j/
cd j
nano
./nano
ls
cd bin
ls
chmod +x nano
$PATH=/home/runner/Ruby-Shell-v137/j/bin/
$PATH=/home/runner/Ruby-Shell-v137/j/bin/
$PATH=/home/runner/Ruby-Shell-v137/j/bin
$PATH=/home/runner/Ruby-Shell-v137/j/bin/nano
$PATH=/home/runner/Ruby-Shell-v137/j/bin/cls
cls
cls
./nano /home/runner/.bashrc
eval "$(/home/runner/Ruby-Shell-v137/posh --init --shell bash --config ~/jandedobbeleer.omp.json)"
eval "$(/home/runner/Ruby-Shell-v137/posh --init --shell ruby --config ~/jandedobbeleer.omp.json)"
help
